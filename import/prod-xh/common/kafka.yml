spring:
  kafka:
    bootstrap-servers: ${spring_kafka_bootstrap_ip:54.254.155.94:9092,54.255.169.143:9092,54.255.190.155:9092}
    consumer:
      group-id: ${spring.application.name}
    producer:
      transaction-id-prefix: txn_
      retries: 3
      acks: all
    admin:
      auto-create: true
      properties:
        auto.create.topics.enable: true

message:
  receiver:
    enabled: ${message_receiver_enabled:false}